// Starter deck — per-slide JavaScript.
//
// Runs inside the sandboxed deck iframe, after Reveal.js and
// window.SlideController are ready. See the README for the full
// SlideController API, or ARCHITECTURE.md for the host/iframe model.
//
// Uncomment and adapt the example below, or delete this file if
// your deck doesn't need any JS.

/*
SlideController.registerSlide('demo', [
  function () {
    // First Enter press on the slide with id="demo"
    var slide = document.getElementById('demo');
    var note = document.createElement('p');
    note.textContent = 'Step 1';
    note.style.color = '#38bdf8';
    note.setAttribute('data-step', '1');
    slide.appendChild(note);
  },
  function () {
    var slide = document.getElementById('demo');
    var note = document.createElement('p');
    note.textContent = 'Step 2';
    note.style.color = '#38bdf8';
    note.setAttribute('data-step', '2');
    slide.appendChild(note);
  },
]);

SlideController.registerReset('demo', function () {
  document.querySelectorAll('[data-step]').forEach(function (el) { el.remove(); });
});
*/
